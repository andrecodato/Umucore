# Configurações centrais do UmuCore.
# Configurações de cada módulo (auth, economy, etc.) ficam em modules/*.yml

debug: false
mostrar-banner: true

# Contatos exibidos pelo comando /umuauth support. Edite livremente.
suporte:
  contatos:
    - "Discord: (configure em config.yml)"
    - "Email: (configure em config.yml)"
